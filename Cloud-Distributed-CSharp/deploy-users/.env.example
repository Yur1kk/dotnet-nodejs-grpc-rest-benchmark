# Copy this file to .env and fill in values for your environment.
# On GCP: leave this file absent — docker-compose.yml defaults to GCP internal IPs automatically.
# Locally (with Docker): no .env is required as it defaults to "postgres" on the same network.
# If you run PostgreSQL separately, uncomment and set the host below:

# DB_HOST=localhost
